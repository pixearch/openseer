/**
 * Fixed height (px) of the frame (Group Nodes) title row. Child positions clamp below this;
 * the header uses the same height so layout and bounds stay aligned.
 */
export const FRAME_HEADER_RESERVE_PX = 52;
